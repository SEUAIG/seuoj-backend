<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seuoj.seuojbackend.mapper.ProblemSetProblemRelMapper">

    <select id="selectByProblemSetId" resultType="com.seuoj.seuojbackend.entity.ProblemSetProblemRel">
        SELECT id, problem_set_id, problem_id, sort_order, is_del
        FROM problem_set_problem_rel
        WHERE problem_set_id = #{problemSetId}
    </select>

    <update id="markDeletedByIds">
        UPDATE problem_set_problem_rel
        SET is_del = 1
        WHERE problem_set_id = #{problemSetId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="restoreByIdsWithSortOrders">
        UPDATE problem_set_problem_rel
        SET is_del = 0,
            sort_order = CASE id
            <foreach collection="rels" item="item">
                WHEN #{item.id} THEN #{item.sortOrder}
            </foreach>
            END
        WHERE problem_set_id = #{problemSetId}
          AND id IN
        <foreach collection="rels" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

    <update id="updateSortOrdersByIds">
        UPDATE problem_set_problem_rel
        SET sort_order = CASE id
        <foreach collection="rels" item="item">
            WHEN #{item.id} THEN #{item.sortOrder}
        </foreach>
        END
        WHERE problem_set_id = #{problemSetId}
          AND id IN
        <foreach collection="rels" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

    <insert id="insertBatch">
        INSERT INTO problem_set_problem_rel (problem_set_id, problem_id, sort_order, is_del)
        VALUES
        <foreach collection="rels" item="item" separator=",">
            (#{item.problemSetId}, #{item.problemId}, #{item.sortOrder}, 0)
        </foreach>
    </insert>

</mapper>
