<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seuoj.seuojbackend.mapper.ClassProblemSetRelMapper">

    <update id="restoreDeletedProblemSetLink">
        UPDATE class_problem_set_rel
        SET is_del = 0
        WHERE id = (
            SELECT t.id
            FROM (
                SELECT id
                FROM class_problem_set_rel
                WHERE class_id = #{classId}
                  AND problem_set_id = #{problemSetId}
                  AND is_del = 1
                ORDER BY id DESC
                LIMIT 1
            ) t
        )
    </update>

</mapper>
