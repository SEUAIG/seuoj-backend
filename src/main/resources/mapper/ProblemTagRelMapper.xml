<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seuoj.seuojbackend.mapper.ProblemTagRelMapper">

    <insert id="insertBatch">
        INSERT INTO problem_tag_rel (problem_id, tag_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.problemId}, #{item.tagId})
        </foreach>
    </insert>

    <update id="markAllDeletedByProblemId">
        UPDATE problem_tag_rel
        SET is_del = 1
        WHERE problem_id = #{problemId}
    </update>

    <update id="restoreByProblemIdAndTagIds">
        UPDATE problem_tag_rel
        SET is_del = 0
        WHERE problem_id = #{problemId}
        AND tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </update>

    <select id="selectTagIdsByProblemIdAndTagIds" resultType="java.lang.Long">
        SELECT tag_id
        FROM problem_tag_rel
        WHERE problem_id = #{problemId}
        AND tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </select>

</mapper>
